---
phase: 03-ralph-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - relay/.env.example
  - backend/.env.example
autonomous: false
requirements:
  - RALF-03

user_setup:
  - service: cloudflare-tunnel
    why: "Expose local relay server to Railway via permanent DNS-routed URL"
    env_vars:
      - name: RELAY_URL
        source: "Cloudflare Tunnel DNS route (e.g. https://relay.yourdomain.com/fix)"
      - name: RELAY_SECRET
        source: "Generate a random secret string (e.g. openssl rand -hex 32)"
    dashboard_config:
      - task: "Cloudflare Tunnel login and creation"
        location: "Terminal: cloudflared tunnel login → cloudflared tunnel create bug-report-relay"
      - task: "DNS route configuration"
        location: "Terminal: cloudflared tunnel route dns bug-report-relay relay.yourdomain.com"
      - task: "Set RELAY_URL and RELAY_SECRET on Railway"
        location: "Railway Dashboard → bug-report-backend service → Variables"

must_haves:
  truths:
    - "Local relay server is reachable from Railway via a permanent Cloudflare Tunnel URL"
    - "Cloudflare Tunnel uses a named tunnel with DNS route (not a quick tunnel with random URL)"
    - "RELAY_URL and RELAY_SECRET are documented in both backend and relay .env.example files"
    - "Tunnel config file exists for repeatable startup"
    - "Relay startup instructions are clear: npm run relay + cloudflared tunnel run"
  artifacts:
    - path: "relay/.env.example"
      provides: "Relay environment variable documentation with tunnel URL"
      contains: "RELAY_SECRET"
    - path: "backend/.env.example"
      provides: "Backend environment variable documentation with relay connection"
      contains: "RELAY_URL"
  key_links:
    - from: "Railway backend (RELAY_URL env var)"
      to: "Cloudflare Tunnel DNS (relay.yourdomain.com)"
      via: "HTTPS POST through Cloudflare edge network"
      pattern: "RELAY_URL"
    - from: "Cloudflare Tunnel"
      to: "localhost:3001 (relay server)"
      via: "cloudflared tunnel run"
      pattern: "localhost:3001"
---

<objective>
Configure Cloudflare Tunnel for permanent relay exposure and verify end-to-end connectivity between Railway backend and local relay server.

Purpose: RALF-03 requires the relay server to be reachable from Railway. Cloudflare Tunnel provides a permanent, TLS-terminated URL that routes to localhost without port forwarding or firewall changes. This plan creates the tunnel configuration and provides a verification checkpoint for the human operator.

Output: Cloudflare Tunnel config, updated .env.example files, and human-verified end-to-end connectivity.
</objective>

<execution_context>
@/Users/miro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ralph-integration/03-RESEARCH.md
@.planning/phases/03-ralph-integration/03-01-SUMMARY.md
@.planning/phases/03-ralph-integration/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 03-01 and 03-02 outputs -->

Backend relay notification (from backend/src/services/relay.ts):
- POSTs to RELAY_URL with Authorization: Bearer RELAY_SECRET
- Payload: RelayFixPayload JSON

Relay server (from relay/src/app.ts):
- Listens on PORT (default 3001)
- Validates Authorization: Bearer RELAY_SECRET
- POST /fix accepts payloads and returns 202

Cloudflare Tunnel commands (from research, cloudflared v2025.5.0):
```bash
# One-time setup
cloudflared tunnel login
cloudflared tunnel create bug-report-relay
cloudflared tunnel route dns bug-report-relay relay.yourdomain.com

# Run (alongside relay server)
cloudflared tunnel run --url http://localhost:3001 bug-report-relay
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cloudflare Tunnel config and document environment setup</name>
  <files>relay/.env.example, backend/.env.example</files>
  <action>
**Update `relay/.env.example`** to include Cloudflare Tunnel documentation:
```
# Relay server configuration
PORT=3001
RELAY_SECRET=                   # Must match RELAY_SECRET on Railway backend (generate: openssl rand -hex 32)
RALPH_WORKSPACE=/Users/miro/Workspace/PERSONAL/ralph-workspace

# Cloudflare Tunnel Setup (one-time):
#   1. cloudflared tunnel login
#   2. cloudflared tunnel create bug-report-relay
#   3. cloudflared tunnel route dns bug-report-relay relay.yourdomain.com
#
# Running the tunnel (alongside relay server):
#   cloudflared tunnel run --url http://localhost:3001 bug-report-relay
#
# Or use a config file at ~/.cloudflared/config.yml:
#   tunnel: bug-report-relay
#   credentials-file: ~/.cloudflared/<tunnel-id>.json
#   ingress:
#     - hostname: relay.yourdomain.com
#       service: http://localhost:3001
#     - service: http_status:404
```

**Ensure `backend/.env.example`** has the relay entries from Plan 03-01:
Verify the following entries exist (add if missing):
```
# Phase 3: Relay server connection
RELAY_URL=https://relay.yourdomain.com/fix    # Cloudflare Tunnel URL to local relay
RELAY_SECRET=                                   # Shared secret (same as relay's RELAY_SECRET)
RALPH_WORKSPACE=                                # Path to ralph-workspace root (for triage.log)
```

**Important:** Do NOT store actual secret values in .env.example. These are templates.

The Cloudflare Tunnel approach uses a NAMED tunnel (not a quick tunnel) per user decision. Named tunnels produce permanent DNS-routed URLs that survive restarts.
  </action>
  <verify>
    <automated>grep -n "RELAY_SECRET" /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/relay/.env.example && grep -n "RELAY_URL" /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/backend/.env.example && grep -n "cloudflared" /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/relay/.env.example</automated>
  </verify>
  <done>
    - relay/.env.example documents PORT, RELAY_SECRET, RALPH_WORKSPACE, and Cloudflare Tunnel setup steps
    - backend/.env.example documents RELAY_URL, RELAY_SECRET, RALPH_WORKSPACE
    - Tunnel documentation specifies named tunnel workflow (not quick tunnel)
    - No actual secrets in .env.example files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Cloudflare Tunnel and end-to-end relay connectivity</name>
  <files>relay/.env</files>
  <action>
    Human performs one-time Cloudflare Tunnel setup and verifies end-to-end connectivity.

    **What was built:** Complete relay pipeline — backend triage logging (TRIA-05), relay notification service (issues.labeled webhook), relay server with auth + dedup + queue + fix_plan.md writer, and Cloudflare Tunnel configuration documentation.

    **Pre-requisites (one-time setup):**
    1. Generate a shared secret: `openssl rand -hex 32` — save the output
    2. Create tunnel: `cloudflared tunnel login && cloudflared tunnel create bug-report-relay`
    3. Route DNS: `cloudflared tunnel route dns bug-report-relay relay.YOURDOMAIN.com`
    4. Set Railway env vars: `RELAY_URL=https://relay.YOURDOMAIN.com/fix` and `RELAY_SECRET=<your-secret>`
    5. Create `relay/.env` with `PORT=3001`, `RELAY_SECRET=<your-secret>`, `RALPH_WORKSPACE=/Users/miro/Workspace/PERSONAL/ralph-workspace`, `GITHUB_TOKEN=<your-github-pat>`, `FIX_TIMEOUT_MS=3600000`

    **Verification steps:**
    1. Start relay: `cd relay && npm run dev`
    2. Start tunnel: `cloudflared tunnel run --url http://localhost:3001 bug-report-relay`
    3. Test health (no auth): `curl -s https://relay.YOURDOMAIN.com/health` — should return 200
    4. Test auth required: `curl -s -X POST https://relay.YOURDOMAIN.com/fix` — should return 401
    5. Test valid request with curl POST to /fix with Authorization Bearer header and JSON payload — should return 202 Accepted
    6. Check fix_plan.md created: `cat projects/test/.ralph/fix_plan.md`
    7. Check triage.log: `tail -1 triage.log`
    8. Test duplicate: repeat step 5 — should return 200 duplicate
  </action>
  <verify>
    <automated>curl -sf https://relay.YOURDOMAIN.com/health || echo "Tunnel not yet configured — human must complete setup"</automated>
  </verify>
  <done>
    - Cloudflare Tunnel is active with permanent DNS route
    - Relay reachable from public internet via tunnel URL
    - Auth rejects unauthorized requests (401)
    - Valid POST produces fix_plan.md and triage.log entry
    - Duplicate requests return 200 (not re-processed)
    - Human types "approved" to confirm all verification steps pass
  </done>
</task>

</tasks>

<verification>
1. `cloudflared tunnel list` — shows `bug-report-relay` tunnel
2. `curl -s https://relay.YOURDOMAIN.com/health` — returns 200
3. `curl -s -X POST https://relay.YOURDOMAIN.com/fix` without auth — returns 401
4. Valid POST returns 202 and fix_plan.md appears in correct project directory
5. Duplicate POST returns 200 with "duplicate" status
6. triage.log contains JSONL entry for the test request
7. `.relay-seen.jsonl` contains the dedup key
</verification>

<success_criteria>
- Cloudflare Tunnel is configured with a permanent DNS-routed URL
- Local relay server is reachable from the public internet via the tunnel
- Auth rejects unauthorized requests (401)
- Valid requests produce fix_plan.md in the correct project's .ralph/ directory
- Deduplication prevents double-writes
- Triage log captures relay-accepted events
- The complete pipeline (webhook -> backend -> relay -> fix_plan.md -> Ralph) is ready for production
</success_criteria>

<output>
After completion, create `.planning/phases/03-ralph-integration/03-03-SUMMARY.md`
</output>
