---
phase: 01-widget
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - widget/src/submit.ts
  - widget/test/widget.test.html
  - widget/dist/widget.js
autonomous: false
requirements:
  - WIDG-01
  - WIDG-08

must_haves:
  truths:
    - "submitReport() builds a FormData with projectId, subject, description, metadata JSON, and screenshot blobs — never sets Content-Type manually"
    - "submitReport() returns { ok: false, message: string } on network failure — never throws"
    - "Running `npm run build` inside widget/ produces dist/widget.js under 150KB uncompressed"
    - "Embedding the script tag in a test HTML page shows the floating bug button without JS errors in the console"
    - "Filling and submitting the form shows a success or error confirmation message inside the widget"
    - "The host page layout and console are unaffected when the widget encounters a simulated error"
  artifacts:
    - path: "widget/src/submit.ts"
      provides: "submitReport() — constructs FormData and POSTs to apiUrl/report"
      exports: ["submitReport"]
    - path: "widget/test/widget.test.html"
      provides: "Manual test page for embedding the built widget"
    - path: "widget/dist/widget.js"
      provides: "Production IIFE bundle (generated by vite build)"
  key_links:
    - from: "widget/src/widget.ts"
      to: "widget/src/submit.ts"
      via: "dynamic import('./submit.js') at submit time"
      pattern: "import.*submit\\.js"
    - from: "widget/test/widget.test.html"
      to: "widget/dist/widget.js"
      via: "<script src='../dist/widget.js' data-project-id='test' data-api-url='http://localhost:3000'>"
      pattern: "data-project-id"
---

<objective>
Complete the widget by implementing the FormData submission module, running the production build, and verifying the full widget flow in a test HTML page.

Purpose: This is the final wire-up. submit.ts connects the form to the backend API. The build confirms Vite produces a single deployable widget.js. The test page confirms the full user flow works end-to-end with visual inspection.
Output: A deployable widget/dist/widget.js file and a verified manual test page demonstrating the complete bug report flow.
</objective>

<execution_context>
@/Users/miro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-widget/01-RESEARCH.md
@.planning/phases/01-widget/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Types from Plan 01 — use directly -->

From widget/src/types.ts:
```typescript
export interface SubmitArgs {
  projectId: string;
  apiUrl: string;
  subject: string;
  description: string;
  metadata: BugMetadata;
  autoScreenshot: Blob | null;
  attachedImages: Blob[];
}

export interface SubmitResult {
  ok: boolean;
  message: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FormData submission module (submit.ts) and production build</name>
  <files>widget/src/submit.ts, widget/dist/widget.js</files>
  <behavior>
    - submitReport() builds FormData: appends projectId, subject, description, metadata (JSON stringified)
    - submitReport() appends autoScreenshot as 'screenshot-auto.jpg' if not null
    - submitReport() appends each attachedImage as 'screenshot-{i}.png'
    - submitReport() does NOT set Content-Type header (browser sets multipart boundary automatically)
    - submitReport() returns { ok: true, message: 'Report submitted successfully' } on HTTP 200
    - submitReport() returns { ok: false, message: text } on non-200 response
    - submitReport() returns { ok: false, message: 'Network error \u2014 please try again' } on fetch throw
    - submitReport() never throws — all errors caught and returned as SubmitResult
  </behavior>
  <action>
Create widget/src/submit.ts:
```typescript
import type { SubmitArgs, SubmitResult } from './types.js';

export async function submitReport({
  projectId,
  apiUrl,
  subject,
  description,
  metadata,
  autoScreenshot,
  attachedImages,
}: SubmitArgs): Promise<SubmitResult> {
  try {
    const form = new FormData();
    form.append('projectId', projectId);
    form.append('subject', subject);
    form.append('description', description);
    form.append('metadata', JSON.stringify(metadata));

    if (autoScreenshot) {
      form.append('screenshots', autoScreenshot, 'screenshot-auto.jpg');
    }

    attachedImages.forEach((blob, i) => {
      form.append('screenshots', blob, `screenshot-${i}.png`);
    });

    // DO NOT set Content-Type — browser sets multipart boundary automatically
    const res = await fetch(`${apiUrl}/report`, {
      method: 'POST',
      body: form,
    });

    if (!res.ok) {
      const text = await res.text().catch(() => '');
      return { ok: false, message: text || `Error ${res.status}` };
    }

    return { ok: true, message: 'Report submitted successfully' };
  } catch (err) {
    // Network failure — never propagate to host page (WIDG-09)
    console.warn('[bug-report-widget] submit failed:', err);
    return { ok: false, message: 'Network error \u2014 please try again' };
  }
}
```

After creating submit.ts, run the production build:
```bash
cd /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget && npm run build
```

The build must produce dist/widget.js. Check the file size:
```bash
wc -c /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget/dist/widget.js
gzip -9 -c /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget/dist/widget.js | wc -c
```

Target: uncompressed under 150KB, gzipped under 60KB. If exceeded, investigate tree-shaking issues.
  </action>
  <verify>
    <automated>cd /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget && node -e "
const fs = require('fs');
const s = fs.readFileSync('src/submit.ts','utf8');
console.assert(s.includes('FormData'), 'FormData missing');
console.assert(s.includes('JSON.stringify(metadata)'), 'metadata JSON stringify missing');
console.assert(!s.includes('Content-Type'), 'MUST NOT manually set Content-Type');
console.assert(s.includes('screenshot-auto.jpg'), 'auto screenshot filename missing');
console.assert(s.includes('Network error'), 'network error fallback missing');
console.assert(s.includes('catch'), 'catch block missing');
console.log('submit.ts OK');
" && test -f dist/widget.js && echo "dist/widget.js exists" && wc -c dist/widget.js</automated>
  </verify>
  <done>submit.ts exports submitReport() with FormData construction (no manual Content-Type), returns SubmitResult, never throws; dist/widget.js produced by npm run build under 150KB uncompressed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete widget flow in browser</name>
  <what-built>
    Complete widget build: widget/dist/widget.js is a single self-executing IIFE file built from all source modules. The widget renders a floating red bug button using Shadow DOM, opens a form pre-filled with the current page URL, captures a screenshot (or falls back gracefully), supports multiple screenshot attachments via file upload and Ctrl+V paste, and shows a success or error confirmation after submission.

    A test HTML page at widget/test/widget.test.html embeds the built widget and includes a mock endpoint simulation note.

    Create widget/test/widget.test.html before this checkpoint:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Bug Report Widget Test</title>
      <!-- Intentional Bootstrap load to test CSS isolation -->
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
      <style>
        body { font-family: sans-serif; padding: 40px; background: #f0f4f8; }
        h1   { color: #1a202c; }
        p    { max-width: 600px; }
        .test-card {
          background: #fff;
          border-radius: 8px;
          padding: 24px;
          box-shadow: 0 2px 8px rgba(0,0,0,0.1);
          max-width: 600px;
        }
      </style>
    </head>
    <body>
      <div class="test-card">
        <h1>Widget Test Page</h1>
        <p>This page tests the bug report widget with Bootstrap loaded to verify CSS isolation.</p>
        <p>The floating red bug button should appear in the bottom-right corner. Click it to open the report form.</p>
        <p><strong>Expected behavior:</strong></p>
        <ul>
          <li>Bug button appears without Bootstrap style conflicts</li>
          <li>Form opens with current URL pre-filled</li>
          <li>Auto-screenshot attempts (may show unavailable on local file)</li>
          <li>Ctrl+V paste of an image works in the form</li>
          <li>Submitting shows "Network error" (no backend running) — this is correct</li>
          <li>Host page is unaffected after any widget error</li>
        </ul>
        <img src="https://via.placeholder.com/400x200" alt="Test cross-origin image for CORS screenshot test">
      </div>

      <!-- Widget embed — data-api-url points to localhost backend (Phase 2) -->
      <script
        src="../dist/widget.js"
        data-project-id="test-project"
        data-api-url="http://localhost:3000">
      </script>
    </body>
    </html>
    ```

    To test: open widget/test/widget.test.html in a browser (use `npx serve widget/` or open directly as file). With a local server: `cd /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget && npx serve . --port 4000` then visit http://localhost:4000/test/widget.test.html
  </what-built>
  <how-to-verify>
    1. Serve the widget directory: `cd /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget && npx serve . --port 4000`
    2. Open http://localhost:4000/test/widget.test.html in your browser
    3. Verify: Red floating bug button appears in the bottom-right corner
    4. Verify: No CSS conflicts with Bootstrap (button looks correct, not Bootstrap-styled)
    5. Click the bug button — form opens
    6. Verify: Page URL field is pre-filled with http://localhost:4000/test/widget.test.html
    7. Verify: No errors in browser DevTools console from the host page (widget warnings with [bug-report-widget] prefix are acceptable)
    8. Fill in subject "Test bug", click Submit — expect "Network error" confirmation (no backend)
    9. Verify: After the error, the host page still functions normally
    10. Verify: DevTools Network tab shows zero requests to api.github.com
    11. (Optional) Ctrl+V an image from clipboard — verify it's listed as attached before submit
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <action>Create widget/test/widget.test.html with Bootstrap CSS loaded; serve widget/ directory; human verifies floating button, CSS isolation, form pre-fill, submission confirmation, and host page integrity.</action>
  <verify><automated>test -f /Users/miro/Workspace/PERSONAL/ralph-workspace/bug-report/widget/test/widget.test.html && echo "test page exists"</automated></verify>
  <done>Human has verified: floating button visible, no Bootstrap conflicts, URL pre-filled, submission shows network error confirmation, host page unaffected, zero api.github.com requests in DevTools</done>
</task>

</tasks>

<verification>
After Plan 03 completion, Phase 1 is done when ALL roadmap success criteria are met:

1. Pasting `&lt;script src="dist/widget.js" data-project-id="..."&gt;&lt;/script&gt;` into any HTML page shows floating button with no style conflicts (WIDG-01, WIDG-02, WIDG-03)
2. Clicking button opens form pre-filled with current page URL; submitting captures screenshot or falls back gracefully (WIDG-04, WIDG-05)
3. Form collects subject, description, browser/OS metadata; reporter sees success or failure confirmation (WIDG-07, WIDG-08)
4. Widget errors do not break or alter the host page (WIDG-09)
5. Multiple screenshots can be attached via file upload or Ctrl+V paste (WIDG-06)
6. dist/widget.js is under 150KB uncompressed
</verification>

<success_criteria>
- submit.ts exports submitReport() using FormData without manual Content-Type header
- submitReport() returns SubmitResult on all code paths (network errors included)
- npm run build produces dist/widget.js under 150KB uncompressed
- test HTML page embeds widget correctly with Bootstrap loaded (CSS isolation verified)
- Human verifies: floating button, form opens, URL pre-filled, submission shows confirmation, host page unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01-widget/01-03-SUMMARY.md` following the summary template.
</output>
