import type { SubmitArgs, SubmitResult } from './types.js';

// Placeholder — implemented in plan 03
export async function submitReport(_args: SubmitArgs): Promise<SubmitResult> {
  return { ok: false, message: 'Not implemented' };
}
